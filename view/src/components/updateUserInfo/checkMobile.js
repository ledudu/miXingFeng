import React from 'react';
import CheckEmailOrMobile from "./checkEmailOrMobile"

const CheckMobile = () => {
	return (
		<CheckEmailOrMobile
			type="mobile"
			self={this}
		/>
	)
}

export default CheckMobile
